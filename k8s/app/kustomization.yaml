apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- configmap.yaml
- deployment.yaml
- service.yaml

# The Argo Workflow's update-manifest step runs, from this directory:
#   kustomize edit set image distopia-registry.distopia.svc.cluster.local:5000/distopia=distopia-registry.distopia.svc.cluster.local:5000/distopia:<short-sha>
# which rewrites the entry below (present once the first bump has happened).
images:
- name: distopia-registry.distopia.svc.cluster.local:5000/distopia
  newName: distopia-registry.distopia.svc.cluster.local:5000/distopia
  newTag: 5db180d8
