datasource db {
    provider = "sqlite"
    url      = "file:./src/lib/db/database.sqlite3"
}

generator client {
    provider = "prisma-client-js"
}

model User {
    id       String @id
    username String
}

model Token {
    id String
    token String

    @@id([id, token])
}
