datasource db {
    provider = "sqlite"
    url      = "file:./db/database.sqlite3"
}

generator client {
    provider = "prisma-client-js"
}

model Token {
    id String
    token String

    @@id([id, token])
}
